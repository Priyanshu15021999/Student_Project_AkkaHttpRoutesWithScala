package Programing

import scala.collection.mutable.ListBuffer

/*
object ProjrammingQus extends App {

  //+++++++++++++++++++++++++++++++++++++++ == bubble Sorting == +++++++++++++++++++++++++++++++++++++++++++++

  val list = ListBuffer(5, 6, 3, 9, 2, 7, 4, 1, 4, 9, 0, 6, 5, 4)
  for (a <- 0 until list.length) {
    for (b <- a + 1 until list.length) {
      if (list(a) > list(b)) {
        val d = list(a)
        list(a) = list(b)
        list(b) = d
      }
    }
  }
  println(list)
*/

//}
  //+++++++++++++++++++++++++++++++++++++++ ==  Array List Object  == +++++++++++++++++++++++++++++++++++++++++++++
